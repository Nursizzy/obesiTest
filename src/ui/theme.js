import { createTheme } from '@mui/material'

export const theme = createTheme({
    palette: {
        primary: {main: '#548235'}, secondary: {main: '#7F7F7F'},
    }, typography: {
        allVariants: {
            color: '#7F7F7F'
        }, fontFamily: 'Raleway',
    }
});